<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>url shortner</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-5">
        <h1>Url Shrinker</h1>
        <form method="POST" class="row" action="/shortenUrl">
            <div class="col-3">
                <input type="text" class="form-control" placeholder="Url" name="fullUrl">
            </div>
            <div class="col-3">
                <button type="submit" class="btn btn-primary mb-3">Shrink</button>
            </div>
            {{#if error}}
            <p class="text-danger">{{error}}</p>
            {{/if}}
        </form>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Full Url</th>
                    <th scope="col">Short Url</th>
                    <th scope="col">Clicks</th>
                </tr>
            </thead>
            <tbody>
                {{#each urls}}
                <tr>
                    <td><a href="">{{this.fullUrl}}</a></td>
                    <form method="POST" action="/increment-clicks">
                        <td>
                            <input type="text" value="{{this._id}}" name="id" hidden="true">
                            <input type="submit" value="{{this.shortUrl}}" style="border: none;">

                        </td>
                    </form>
                    <td>{{this.click}}</td>
                </tr>
                {{/each}}

            </tbody>
    </div>
    {{!--
    <script src="js/app.js"></script> --}}
</body>

</html>